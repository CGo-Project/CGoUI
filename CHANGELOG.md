# Changelog

## 2.1.0 - 2026-08-06

- 将 CGoUI 源码、主题样式、文档和发布产物迁移为独立仓库结构。
- 构建产物统一写入 `dist/`，不再依赖宿主项目的 `cgoui/` 目录。
- 修复主题引擎对仓库外部 CSS 路径的隐式依赖，安装后可以直接使用。
- 增加 NPM、CDN、打包检查、烟囱测试和发布脚本。
- 将备案、版权等宿主应用信息改为可配置的兼容垫片，不再写死在通用组件库中。

## 2.0.9

- 现有公开 NPM 版本，保留原有组件 API 和 React 入口。
